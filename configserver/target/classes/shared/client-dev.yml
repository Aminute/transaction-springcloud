server:
  port: 8030
Spring:
  application:
    name: client
  thymeleaf:
    prefix: classpath:/static/
    suffix: .jsp
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true
